$(document).getElementById("toobin").onclick = function()
{
    



/*
$(document).ready(function() {
        $.getJSON("toobin.json", function(data)
        {
            $.each(data, function()
            {
                $.each(this, function(key, value) 
                {
                    $("#speaker").append(
                        value.title,
                        value.month,
                        value.speaker,
                        value.image,
                        value.text
                    );
                });
            });
        });
    }); // end ready
}
*/